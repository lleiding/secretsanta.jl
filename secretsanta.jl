using Random

names = ["John",
"James",
"Kent",
"Judy"]

pairs = [1, 1, 2, 2]

santaindex = shuffle(eachindex(names))
while any(pairs.== pairs[wichtelidx])
    santaindex = shuffle(eachindex(names))
    end

secretsanta = names[wichtelidx]
